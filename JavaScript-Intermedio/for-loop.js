const prompt = require("prompt-sync")({ sigint: true });

for (let i = 0; i <= 10; i += 2) {
    console.log("iteracion nro:", i);
    // utilizamos los numeros indices pares 
}

// SENTIDO INVERSO
for (let i = 10; i >= 0; i -= 2) {
    console.log("iteracion nro:", i);
    // utilizamos los numeros indices pares 
}